{
  "version": 2,
  "builds": [
    {
      "src": "api/mcp.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/mcp",
      "methods": ["GET", "POST", "DELETE", "OPTIONS"],
      "dest": "/api/mcp"
    },
    {
      "src": "/health",
      "dest": "/api/mcp"
    }
  ]
}
